const errorHandler = require('./error')
const notFoundHandler = require('./notFound')
const validateHanlder = require('./validate')

module.exports = {
  errorHandler,
  notFoundHandler,
  validateHanlder
}
